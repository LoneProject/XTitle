plugins {
    id "java"
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = "UTF-8"
}

tasks.jar {
    archiveFileName.set("XTitle-API.jar")
}

group = "org.lone64.xtitle"

dependencies {
    compileOnly "io.papermc.paper:paper-api:1.17-R0.1-SNAPSHOT"
}